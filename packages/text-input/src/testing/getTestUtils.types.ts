import { type FormElements, type FormUtils } from '@az-tools/test-harnesses';

type TextInputElements = FormElements;
type TextInputUtils = FormUtils;

export type TestUtilsReturnType = TextInputElements & TextInputUtils;
