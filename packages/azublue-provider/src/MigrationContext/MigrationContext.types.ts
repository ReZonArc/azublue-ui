export interface MigrationContextType {
  /**
   * Determines globally if popover elements using `Popover` component from `@azublue-ui/popover` package should render in top layer
   * @internal
   */
  forceUseTopLayer?: boolean;
}
